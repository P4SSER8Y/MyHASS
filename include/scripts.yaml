blink_the_lamp:
  alias: blink the lamp
  sequence:
    - service: light.toggle
      data:
        entity_id: light.desktop_lamp
    - delay:
        milliseconds: 500
    - service: light.toggle
      data:
        entity_id: light.desktop_lamp
    - delay:
        milliseconds: 500
    - service: light.toggle
      data:
        entity_id: light.desktop_lamp
    - delay:
        milliseconds: 500
    - service: light.toggle
      data:
        entity_id: light.desktop_lamp
    - delay:
        milliseconds: 500
    - service: light.toggle
      data:
        entity_id: light.desktop_lamp
    - delay:
        milliseconds: 500
    - service: light.toggle
      data:
        entity_id: light.desktop_lamp
  
guten_abend:
  alias: Guten Abend!
  sequence:
    - delay:
        seconds: 60
    - service: light.turn_off
      data:
        entity_id: light.desktop_lamp

